--- a/scp.c
+++ b/scp.c
@@ -628,8 +628,10 @@ main(int argc, char **argv)
 	if (iamremote)
 		mode = MODE_SCP;
 
+#ifndef __OHOS__
 	if ((pwd = getpwuid(userid = getuid())) == NULL)
 		fatal("unknown user %u", (u_int) userid);
+#endif
 
 	if (!isatty(STDOUT_FILENO))
 		showprogress = 0;
@@ -1679,7 +1681,9 @@ sink(int argc, char **argv, const char *src)
 	setimes = targisdir = 0;
 	mask = umask(0);
 	if (!pflag) {
+#ifndef __OHOS__
 		mask |= 07000;
+#endif
 		(void) umask(mask);
 	}
 	if (argc != 1) {
