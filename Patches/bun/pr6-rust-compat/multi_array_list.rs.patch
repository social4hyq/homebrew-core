diff --git a/src/collections/multi_array_list.rs b/src/collections/multi_array_list.rs
index 883f3d728..335fc085d 100644
--- a/src/collections/multi_array_list.rs
+++ b/src/collections/multi_array_list.rs
@@ -344,10 +344,9 @@ impl<T> Reflected<T> {
         let mut i = 0;
         while i < n {
             let f = &fields[i];
-            let size = match f.ty.info().size {
-                Some(s) => s,
-                None => panic!("MultiArrayList: field type must be Sized"),
-            };
+            let size = f.ty
+                .size()
+                .expect("MultiArrayList: field type must be Sized");
             let align = align_sort_key(size, struct_align);
             out[i] = FieldMeta {
                 size,
