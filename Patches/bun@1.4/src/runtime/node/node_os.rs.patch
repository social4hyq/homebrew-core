diff --git a/src/runtime/node/node_os.rs b/src/runtime/node/node_os.rs
index 4ce266374cd05f0dcd65022b471f74eafd753b00..3ab070f051a42e81355183082d96331bed83dda1 100644
--- a/src/runtime/node/node_os.rs
+++ b/src/runtime/node/node_os.rs
@@ -724,6 +724,14 @@ mod _impl {
             let _ = using_heap;
 
             if ret != 0 {
+                #[cfg(target_env = "ohos")]
+                if ret == bun_sys::E::ENOENT as c_int {
+                    return Ok(BunString::from_bytes(
+                        env_var::HOME
+                            .get_not_empty()
+                            .unwrap_or(b"/data/storage/el2/base"),
+                    ));
+                }
                 return Err(global.throw_value(
                     bun_sys::Error::from_code(
                         // `ret` is a libc errno; a code outside the table is `EUNKNOWN`.
@@ -737,12 +745,20 @@ mod _impl {
             if result.is_null() {
                 // bionic has no passwd entries for app uids; with HOME also unset
                 // (zygote/run-as), return a usable default rather than throwing.
+                #[cfg(target_env = "ohos")]
+                {
+                    return Ok(BunString::from_bytes(
+                        env_var::HOME
+                            .get_not_empty()
+                            .unwrap_or(b"/data/storage/el2/base"),
+                    ));
+                }
                 #[cfg(target_os = "android")]
                 {
                     return Ok(BunString::static_("/data/local/tmp"));
                 }
                 // in uv__getpwuid_r, null result throws UV_ENOENT.
-                #[cfg(not(target_os = "android"))]
+                #[cfg(not(any(target_os = "android", target_env = "ohos")))]
                 return Err(global.throw_value(
                     bun_sys::Error::from_code(bun_sys::E::ENOENT, bun_sys::Tag::uv_os_homedir)
                         .to_js(global),
