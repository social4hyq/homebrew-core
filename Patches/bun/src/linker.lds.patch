diff --git a/src/linker.lds b/src/linker.lds
index 401cc35b60..5443f695e6 100644
--- a/src/linker.lds
+++ b/src/linker.lds
@@ -4,6 +4,27 @@ BUN_1.2 {
 	node_api_*;
 	node_module_register;
 
+	/* 18 shim interposer symbols, exported so dlopen'd native addons resolve them from the executable; keep 1:1 with scripts/build/shims/ohos_compat_shim.c — a missing symbol means the addon bypasses the shim. */
+	/* close and the epoll trio share one shim-internal fd registry and must move together. */
+	syscall;
+	close_range;
+	getcwd;
+	getpwuid_r;
+	tmpfile;
+	linkat;
+	link;
+	symlinkat;
+	splice;
+	getaddrinfo;
+	poll;
+	ppoll;
+	dup2;
+	dup3;
+	close;
+	epoll_ctl;
+	epoll_wait;
+	epoll_pwait;
+
 
   uv_accept;
   uv_async_init;
