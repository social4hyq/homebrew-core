diff --git a/scripts/build/deps/mimalloc.ts b/scripts/build/deps/mimalloc.ts
index 5e491a9abd..1c8b78d9f1 100644
--- a/scripts/build/deps/mimalloc.ts
+++ b/scripts/build/deps/mimalloc.ts
@@ -68,6 +68,7 @@ export const mimalloc: Dependency = {
     // spawned children keep the system THP policy. JSC's reservations and
     // bun_alloc's lazy arena opt out the same way on their side.
     if (cfg.linux) defines.MI_DEFAULT_ALLOW_THP = 0;
+    if (cfg.abi === "ohos") defines.MI_NO_SET_VMA_NAME = 1;
 
     if (cfg.abi === "musl") defines.MI_LIBC_MUSL = 1;
     if (override) defines.MI_MALLOC_OVERRIDE = true;
