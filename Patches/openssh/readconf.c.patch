diff --git a/readconf.c b/readconf.c
index 0218ed1..e89cb0c 100644
--- a/readconf.c
+++ b/readconf.c
@@ -2637,8 +2637,12 @@ read_config_file_depth(const char *filename, struct passwd *pw,
 
 		if (fstat(fileno(f), &sb) == -1)
 			fatal("fstat %s: %s", filename, strerror(errno));
+#ifdef __OHOS__
+		if ((sb.st_mode & 002) != 0)
+#else
 		if (((sb.st_uid != 0 && sb.st_uid != getuid()) ||
 		    (sb.st_mode & 022) != 0))
+#endif
 			fatal("Bad owner or permissions on %s", filename);
 	}
 
