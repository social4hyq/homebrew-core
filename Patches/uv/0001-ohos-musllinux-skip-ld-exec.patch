--- a/crates/uv-python/python/packaging/_musllinux.py	2026-08-12 12:06:25.636000000 +0000
+++ b/crates/uv-python/python/packaging/_musllinux.py	2026-08-12 12:06:35.844000000 +0000
@@ -47,8 +47,8 @@
         return None
     if ld is None or "musl" not in ld:
         return None
-    proc = subprocess.run([ld], stderr=subprocess.PIPE, text=True)
-    return _parse_musl_version(proc.stderr)
+    # OHOS: sandbox can't execve() the linker, so hardcode musl 1.2 (matches python@3.14's pip).
+    return _MuslVersion(major=1, minor=2)
 
 
 def platform_tags(archs: Sequence[str]) -> Iterator[str]:
