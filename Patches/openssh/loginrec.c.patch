--- a/loginrec.c	2026-06-05 04:01:45.127286268 +0000
+++ b/loginrec.c	2026-06-05 04:02:02.078359254 +0000
@@ -294,8 +294,13 @@
 	 */
 	pw = getpwuid(uid);
 	if (pw == NULL)
+#ifdef __OHOS__
+		/* OHOS may not have traditional passwd entries */
+		return NULL;
+#else
 		fatal("%s: Cannot find account for uid %ld", __func__,
 		    (long)uid);
+#endif
 
 	if (strlcpy(li->username, pw->pw_name, sizeof(li->username)) >=
 	    sizeof(li->username)) {
@@ -367,8 +372,13 @@
 		strlcpy(li->username, username, sizeof(li->username));
 		pw = getpwnam(li->username);
 		if (pw == NULL) {
+#ifdef __OHOS__
+			li->uid = (uid_t)-1;
+			return 0;
+#else
 			fatal("%s: Cannot find user \"%s\"", __func__,
 			    li->username);
+#endif
 		}
 		li->uid = pw->pw_uid;
 	}
