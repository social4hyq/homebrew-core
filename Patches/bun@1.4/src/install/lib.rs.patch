diff --git a/src/install/lib.rs b/src/install/lib.rs
index 314523ca5e..efccc7252e 100644
--- a/src/install/lib.rs
+++ b/src/install/lib.rs
@@ -418,6 +418,10 @@ impl RunCommand {
             "/private/tmp"
         } else if cfg!(target_os = "android") {
             "/data/local/tmp"
+        } else if cfg!(target_env = "ohos") {
+            // OHOS app-level cache directory; this constant is evaluated
+            // before any TMPDIR environment lookup.
+            "/data/storage/el2/base/cache"
         } else {
             "/tmp"
         };
