diff --git a/src/runtime/cli/create/SourceFileProjectGenerator.rs b/src/runtime/cli/create/SourceFileProjectGenerator.rs
index 3301ba5e05..65b837e6a2 100644
--- a/src/runtime/cli/create/SourceFileProjectGenerator.rs
+++ b/src/runtime/cli/create/SourceFileProjectGenerator.rs
@@ -67,7 +67,9 @@ pub(crate) fn generate(
     // Add Tailwind dependencies if needed
     if needs_to_inject_tailwind {
         result.dependencies.insert(b"tailwindcss")?;
-        result.dependencies.insert(b"bun-plugin-tailwind")?;
+        // Upstream bun-plugin-tailwind calls oxide APIs/symbols removed from current oxide; the @ohos-ports rewrite targets current oxide + the openharmony binding, and its non-statically-resolvable lightningcss require needs the alias too.
+        result.dependencies.insert(b"bun-plugin-tailwind@npm:@ohos-ports/bun-plugin-tailwind@0.1.2")?;
+        result.dependencies.insert(b"lightningcss@npm:@ohos-ports/lightningcss@1.32.0")?;
     }
 
     // Add shadcn-ui dependencies if needed
