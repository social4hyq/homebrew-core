diff --git a/platform.c b/platform.c
index fd1a7a7..fe0c599 100644
--- a/platform.c
+++ b/platform.c
@@ -160,7 +160,7 @@ platform_locked_account(struct passwd *pw)
 {
 	int locked = 0;
 	char *passwd = pw->pw_passwd;
-#ifdef USE_SHADOW
+#if defined(USE_SHADOW) && !defined(__OHOS__)
 	struct spwd *spw = NULL;
 #ifdef USE_LIBIAF
 	char *iaf_passwd = NULL;
