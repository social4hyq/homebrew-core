name: bottle-build

# Readable Actions-list entries (upstream dispatch-rebottle.yml pattern) —
# without this every run is just "bottle-build" and telling a llvm@21
# publish from a codex dry-run means opening each one.
run-name: "bottle-build: ${{ inputs.formula }} (upload=${{ inputs.upload }})"

# OHOS bottle pipeline: GitHub-hosted ARM runner + OHOS container.
# Logic lives in .github/scripts/; this file is orchestration only.
#
# Manual: Actions → bottle-build → Run workflow
#   upload=false (default): build + bottle + workflow artifact (no repo changes)
#   upload=true: upload to atomgit release (tag <formula>-v<ver>-rN), merge the
#     bottle block back, push to push_ref (default main) + atomgit main,
#     verify poured_from_bottle
#   reason (optional): freeform note for a rebottle-only run (no version
#     bump — bottle.sh's -rN auto-increment already handles that case, this
#     just records *why*). Lands in the atomgit release body + commit
#     message body. Not required for a normal version-bump build.
# Also callable via workflow_call:
#   pr-validate.yml invokes this per changed formula on pull_request, always
#     upload=true with push_ref set to the PR's own head branch — the bottle
#     is built and published DURING review and pushed back onto the PR
#     branch itself, so by the time a human merges, main already contains a
#     bottled formula (2026-07-20: replaces the old build-after-merge design,
#     which had a self-trigger rebuild-loop bug — see detect-changes.sh).
#   publish-on-merge.yml invokes this per changed formula on push to main
#     (upload=true, push_ref defaults to main) — now mostly a safety net for
#     edge cases (e.g. a merge that landed without going through pr-validate)
#     since the common case already has its bottle from the PR branch.
#
# TODO (low priority): M3 release GET||POST 409 race; L2 upload=false
# artifacts have a fake root-url, never merge them manually.

on:
  workflow_dispatch:
    inputs:
      formula:
        description: 'Formula name (e.g. icu4c@78 / bun / llvm@21 / claude-code)'
        required: true
        type: string
      upload:
        description: 'Upload bottle to atomgit release and write back the bottle block (pushes both remotes)'
        required: true
        type: boolean
        default: false
      push_ref:
        description: 'Branch to push the bottle commit to (default: main)'
        required: false
        type: string
        default: 'main'
      reason:
        description: 'Why (optional, e.g. "rebottle: runner image bump" — recorded in the atomgit release body + commit message, borrowed from upstream homebrew-core''s dispatch-rebottle.yml audit trail)'
        required: false
        type: string
        default: ''
  workflow_call:
    inputs:
      formula:
        required: true
        type: string
      upload:
        required: false
        type: boolean
        default: false
      push_ref:
        required: false
        type: string
        default: 'main'
      reason:
        required: false
        type: string
        default: ''

defaults:
  run:
    shell: bash -euo pipefail {0}

permissions:
  contents: write

concurrency:
  # event_name in the group keeps auto (push) runs from evicting a queued
  # manual publish run of the same formula
  group: bottle-build-${{ github.event_name }}-${{ inputs.formula }}
  cancel-in-progress: false

env:
  IMAGE: ghcr.io/social4hyq/ci-runner@sha256:38e740c8e355b2ccc9f34ba4bcab955a3fbc076278ae4bd3a07ea3707e90bb56 # :latest, 2026-07-19
  CONTAINER: ohos
  TAP_IN_CONTAINER: /storage/Users/currentUser/.harmonybrew/Homebrew/Library/Taps/social4hyq/homebrew-core
  # Inputs pass through env (never interpolated into shell) and a whitelist check
  FORMULA: ${{ inputs.formula }}
  UPLOAD: ${{ inputs.upload }}
  PUSH_REF: ${{ inputs.push_ref }}
  RUN_NUMBER: ${{ github.run_number }}

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360
    steps:
      # pull_request's default checkout ref is the synthetic base+head merge
      # commit; when we're about to push a bottle commit back (upload=true,
      # push_ref = the PR's own branch), we need the actual branch tip
      # instead so the push lands cleanly. Empty string falls back to
      # actions/checkout's normal per-event default for every other trigger.
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || '' }}
          fetch-depth: 0

      - name: Validate input
        run: |
          [[ "$FORMULA" =~ ^[A-Za-z0-9@+._-]+$ ]] \
            || { echo "::error::invalid formula name: $FORMULA (allowed: [A-Za-z0-9@+._-])"; exit 1; }

      - name: Start OHOS container
        run: .github/scripts/setup-container.sh

      - name: Build from source
        run: .github/scripts/build.sh

      - name: brew test gate
        run: .github/scripts/test-gate.sh

      - name: Failure diagnostics
        if: failure()
        run: |
          echo "== brew logs =="
          docker exec "$CONTAINER" bash -lc \
            'for f in /root/.cache/Homebrew/Logs/*/*; do echo "--- $f"; tail -30 "$f"; done' || true

      - name: Bottle
        id: bottle
        env:
          ATOMGIT_TOKEN: ${{ secrets.ATOMGIT_TOKEN }}
        run: .github/scripts/bottle.sh

      - name: Upload bottle as artifact
        if: success() && !inputs.upload
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: bottle-${{ inputs.formula }}-run${{ github.run_number }}
          path: bottle-out/
          retention-days: 14

      - name: Publish to atomgit and push
        id: publish
        if: inputs.upload
        env:
          ATOMGIT_TOKEN: ${{ secrets.ATOMGIT_TOKEN }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # main requires PRs and GITHUB_TOKEN can't bypass that ruleset, so
          # every push (main or a PR branch) uses this admin PAT instead —
          # one code path, no branching on which ref needs which token.
          BOT_PUSH_TOKEN: ${{ secrets.BOT_PUSH_TOKEN }}
          TAG: ${{ steps.bottle.outputs.tag }}
          REASON: ${{ inputs.reason }}
        run: .github/scripts/publish.sh

      - name: Verify poured bottle
        id: verify
        if: inputs.upload
        run: .github/scripts/verify-poured.sh

      # publish failed before the bottle commit reached main → the release is
      # orphaned and safe to delete
      - name: Roll back atomgit release
        if: failure() && inputs.upload && steps.publish.outcome == 'failure'
        env:
          ATOMGIT_TOKEN: ${{ secrets.ATOMGIT_TOKEN }}
          TAG: ${{ steps.bottle.outputs.tag }}
        run: .github/scripts/rollback-release.sh

      # verify failed after main was pushed → main references the bad tag;
      # auto-deleting would make it worse, needs a human decision
      - name: Bad release advisory
        if: failure() && inputs.upload && steps.verify.outcome == 'failure'
        env:
          TAG: ${{ steps.bottle.outputs.tag }}
        run: |
          echo "- ❌ published tag $TAG failed verification: revert the bottle commit on main and delete the release, or fix forward and re-run" >> "$GITHUB_STEP_SUMMARY"
